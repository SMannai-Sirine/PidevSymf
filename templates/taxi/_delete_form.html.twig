<form method="post" action="{{ path('taxi_delete', {'id': taxi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taxi.id) }}">
    <button class="btn">Delete</button>
</form>
