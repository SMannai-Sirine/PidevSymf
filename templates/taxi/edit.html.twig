{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Taxi{% endblock %}

{% block body %}
    <h1>Edit Taxi</h1>

    {{ include('taxi/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-success" href="{{ path('taxi_index') }}">back to list</a>

    {{ include('taxi/_delete_form.html.twig') }}
{% endblock %}
