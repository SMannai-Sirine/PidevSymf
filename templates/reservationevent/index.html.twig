{% extends 'base.html.twig' %}

{% block title %}Reservationevent index{% endblock %}

{% block body %}
    <h1>Reservationevent index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dateevent</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for reservationevent in reservationevents %}
            <tr>
                <td>{{ reservationevent.id }}</td>
                <td>{{ reservationevent.dateevent ? reservationevent.dateevent|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_reservationevent_show', {'id': reservationevent.id}) }}">show</a>
                    <a href="{{ path('app_reservationevent_edit', {'id': reservationevent.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_reservationevent_new') }}">Create new</a>
{% endblock %}
