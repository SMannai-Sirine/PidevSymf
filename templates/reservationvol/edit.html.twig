{% extends 'base.html.twig' %}

{% block title %}Edit Reservationvol{% endblock %}

{% block body %}
    <h1>Edit Reservationvol</h1>

    {{ include('reservationvol/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reservationvol_index') }}">back to list</a>

    {{ include('reservationvol/_delete_form.html.twig') }}
{% endblock %}
{% block listFront %}
    <li class=""><a href="{{ path('app_reservationvol_index') }}">Mes Reservations</a></li>
    <li class=""><a href="{{ path('app_vol_index_front') }}">Vols</a></li>
{% endblock %}