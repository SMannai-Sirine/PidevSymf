<form method="post" action="{{ path('app_utilisateur_delete', {'idu': utilisateur.idu}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.idu) }}">
    <button class="btn">Delete</button>
</form>
